//What will be the  type of Array
// its object


// rest parameter ex rest parameter does convert all argument passes in function it can store in single value.
// it takes argument as an array

function a(...abc){
    console.log(abc)
}
a(8,9,10);

// Can you tell typeofNan is called NaN it type is number


// Can you tell me Temporaildead Zone ?
// in TMZ what happen with let and const are happen in TMZ it shows error before initalize we cannot acces it shows reference error and in in var it shows undefined

// console.log(b);
// const b = 4;

// console.log(c);
// let c = 5;


console.log(d);
var d = 5;

const str = "Yash Mane".split("").reverse().join("");
console.log(str);


// 1 - array type is object 
// 2- if having empty array is also object and all object are trutiy value


if([]== true){             // it will convert the value (true its boolean value it convert into number) true = 1 false = 0 
console.log("i am yash") 
}
else{
console.log("no one is present")
}


// React 

//  React key we can use in list  === for identify the items for adding removing or change  with the help of unique key means indexing .

// tell me for  routing which packing will you use -- react-router-dom
// for dynamic routing which we install same package but passing param  user:id

// which hook you use
// i can use useState,useEffect,usememo,usecustomhook,useref,usecallback




//what is the use of usememo 

// it return memorize value , in heavy calculation we can use usememo


// can you tell me virtual dom is good or not it is good beacuse it is update only updated code not all component will be load 


//how do you validate user input in node js ---  (express-validator, joi)


//can you tell  me what is use of reducer method



// A reducer is a pure function that determines how the state of your application changes in response to an action.

// It takes two inputs:

// state → the current state of the application (or a slice of it)

// action → an object describing what happened ({ type: "ACTION_TYPE", payload: data })

// And it returns a new state.


//differnce div and span 
// div is block  level element and span is inline level element 

// // what is HTML AND XHTML

// | Feature          | HTML                                                     | XHTML                                                |
// | ---------------- | -------------------------------------------------------- | ---------------------------------------------------- |
// | Tag closing      | Optional for some tags (`<br>`)                          | Mandatory for all tags (`<br />`)                    |
// | Case sensitivity | Tag names are **not case-sensitive** (`<DIV>` = `<div>`) | Tag names **must be lowercase** (`<div>`)            |



// can you tell em and i tag

// em is empaszhis text its having meaning and i tag is italic style


// can you tell me alt attribute
// it is  use in image tag for if image was not show in broweser it shows name

// can you tell me css border and css outline


// Feature	                Border              	           Outline
// Placement	Inside the element’s box	           Outside the element’s border
// Affects    layout	Yes (affects element’s size)	   No (does not affect size or layout)
// Shorthand	border: 2px solid red;               	outline: 2px solid blue;
// Rounding	Follows border-radius               	Does NOT follow border-radius




  
